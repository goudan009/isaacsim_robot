# 空场景性能评测

本报告只记录空场景基线，不包含机器人、`ros2_control` 或传感器。格式和测评线见
[`../../docs/REPORTING_CN.md`](../../docs/REPORTING_CN.md)。

## 当前结论

采用最新有效批次：`20260909-102402-空场景-codex`。

| 结论维度 | 结论 | 说明 |
| --- | --- | --- |
| 时间有效性 | **通过** | 无仿真时间回退和重复时间戳 |
| 实时仿真 | **通过** | RTF 为 `1.977`，高于 `1.0` |
| 机器人控制能力 | 不适用 | 空场景不启动机器人或控制器 |

## 测评线

| 指标 | 通过线 | 说明 |
| --- | --- | --- |
| 仿真时间回退 | `0` | 有回退时批次无效 |
| 重复时间戳 | `0` | 有重复时批次无效 |
| RTF | 不低于 `1.0` | 当前空场景实时参考线 |
| physics step rate | 仅记录 | 用于与机器人或传感器增量对比 |

## 最新有效批次

### 批次：20260909-102402-空场景-codex

| 字段 | 内容 |
| --- | --- |
| 执行时间 | 2026-09-09 10:24:02 +08:00 |
| 执行者 | codex |
| 版本 | 未记录 |
| 测试范围 | 空场景；不生成机器人；不启动控制器或传感器 |
| 配置 | headless；physics `120 Hz`；render `60 Hz`；real loop `120 Hz`；窗口约 `10 s` |
| 功能结论 | 不适用 |
| 实时结论 | 通过 |

| 指标 | 通过线 | 实测值 | 判定 |
| --- | --- | ---: | --- |
| 仿真时间回退 | 0 | 0 | 通过 |
| 重复时间戳 | 0 | 0 | 通过 |
| `/clock` 接收频率 | 仅记录 | 237.252 Hz | 仅记录 |
| physics step rate | 仅记录 | 237.252 Hz | 仅记录 |
| RTF | 不低于 1.0 | 1.977 | 通过 |

结论：当前机器在相同 Isaac 启动适配器下，空场景可以超过实时运行。该结果只作为负载基线；
机器人控制或传感器场景低于此值时，不能据此直接归因到单一模块。

原始证据：`raw/empty_scene_performance.json`、`raw/empty_scene_performance.launch.log`。

## 复测入口

```bash
python3 test/performance/empty_scene_performance.py
```

## 写入模板

后续批次直接使用 [`../../docs/REPORTING_CN.md`](../../docs/REPORTING_CN.md) 的统一模板。
